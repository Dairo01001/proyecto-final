package util;

public class Constantes {
    public static final String DIR = System.getProperty("user.dir");
    public static final String SEPARATOR = System.getProperty("os.name").equals("Linux") ? "/" : "\\";
}
